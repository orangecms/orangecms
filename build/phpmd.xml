<?xml version="1.0"?>
<ruleset name="Ruleset for Orange CMS"
         xmlns="http://pmd.sf.net/ruleset/1.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sf.net/ruleset/1.0.0
                     http://pmd.sf.net/ruleset_xml_schema.xsd"
         xsi:noNamespaceSchemaLocation="
                     http://pmd.sf.net/ruleset_xml_schema.xsd">
    <description>
        This is the PHP Mess Detector ruleset configuration for Orange CMS.
        Don't mess with it.
    </description>

    <!-- Import the entire cleancode ruleset -->
    <rule ref="rulesets/cleancode.xml" />

    <!-- Import the entire codesize ruleset -->
    <rule ref="rulesets/codesize.xml" />

    <!-- Import the entire controversial ruleset -->
    <rule ref="rulesets/controversial.xml" />

    <!-- Import the entire design ruleset -->
    <rule ref="rulesets/design.xml" />

    <!-- Import the entire naming ruleset -->
    <rule ref="rulesets/naming.xml" />

    <!-- Import the entire unused code rule set -->
    <rule ref="rulesets/unusedcode.xml" />

</ruleset>
